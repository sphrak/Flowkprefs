dist: bionic
language: android
cache:
  directories:
  - "$HOME/.gradle/caches/"
  - "$HOME/.gradle/wrapper/"
android:
  components:
    - build-tools-29.0.2
    - android-29
before_install:
- chmod +x gradlew
script:
- "./gradlew clean build"
- "./gradlew test"
deploy:
  provider: script
  script: ./gradlew bintrayUpload
  skip_cleanup: true
  on:
    tags: true
    repo: sphrak/Flowkprefs
